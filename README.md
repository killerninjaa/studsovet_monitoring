## Using
```
1. docker-compose up -d
2. docker-compose run odoo bash
3. odoo --init base --database dictator-plus --stop-after-init --db_host=postgres --db_user dictator-plus --db_password a8c9e18e0fee1da96961a930277be6e1
4. exit
5. docker-compose restart
```